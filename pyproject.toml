[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tiktok-live-recorder"
version = "8.6.0"
description = "A tool for recording TikTok live streams"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "telethon>=1.42.0",
    "distro>=1.9.0",
    "curl_cffi>=0.12.0",
    "requests>=2.32.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.12.0",
    "pre-commit>=4.3.0",
    "pytest",
    "pytest-cov",
    "httpx>=0.28.0",
]

[project.urls]
Homepage = "https://github.com/ne0lith/tiktok-live-recorder"
Repository = "https://github.com/ne0lith/tiktok-live-recorder"
Issues = "https://github.com/ne0lith/tiktok-live-recorder/issues"

[project.scripts]
tiktok-live-recorder = "tiktok_live_recorder.cli:run"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"tiktok_live_recorder.web" = ["static/*"]

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
testpaths = [
    "tests",
]
